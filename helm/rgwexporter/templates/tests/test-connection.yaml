apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rgwexporter.fullname" . }}-test-connection"
  labels:
    {{- include "rgwexporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rgwexporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
